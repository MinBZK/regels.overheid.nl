query BlogsForView {
  blogs(pagination: { pageSize: 9999 }, sort: "publishedAt:desc") {
    id: documentId
    title
    category
    publishedAt
    cover {
      url
      alternativeText
    }
  }
}
